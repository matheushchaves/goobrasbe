apply plugin: "war"

bootWar {
    mainClassName = "br.com.goobras.GoobrasbeApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
